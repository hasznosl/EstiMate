import netWorthOverTimeFixture from "./netWorthOverTimeFixture";

const netWorthOverTimeToFutureFixture = {
  ...netWorthOverTimeFixture,
  // it may need to be expanded later with future data
}

export default netWorthOverTimeToFutureFixture